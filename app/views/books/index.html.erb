<div>
  <div>
    <div>
      <%= render 'books/list', book: @book %>
      <div>
        <h1>ComiC</h1>
        <table>
          <thead>
            <tr>
            <td></td>
            <td>title</td>
            <td>Impressions</td>
            </tr>
          </thead>
          <tbody>
            <% @books.each do |book| %>
            <tr>
              <td></td>
              <td><%= book.title %></td>
              <td><%= book.body %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>