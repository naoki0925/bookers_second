<div>
  <h2>ComiC detail</h2>
  <%= render 'books/list', book: @book %>
  <table>
    <tbody>
      <tr>
        <td><%= @books.title %></td>
        <td><%= @books.body %></td>
      </tr>
      <%= link_to "編集", edit_book_path(@books) %>
      or
      <%= link_to "戻る", books_path %>
    </tbody>
  </table>
</div>